# Acceptance Test — Migration 011 → 020

## Trạng thái

- PostgreSQL target: 18+
- Migration 001 → 010: đã verify PASS trên PostgreSQL 18.6
- Migration 011 → 020: acceptance suite đã được tạo, **chưa chạy**
- AT-011 → AT-022: phải PASS trước khi mở gate 021 → 030

## Phạm vi

- Resource hierarchy cùng organization.
- Resource permission cùng organization.
- Resource account ownership/provider consistency.
- Data Package owner/version/resource/grant tenant integrity.
- Device → Resource tenant integrity.
- User Session user/device tenant integrity và temporal CHECK.
- Device capability uniqueness.

## Cách chạy

```powershell
Get-Content .\database\tests\acceptance_011_020.sql | docker exec -i workspace-ai-agent-postgres psql -U workspace -d workspace_ai_agent -v ON_ERROR_STOP=1
```

Kỳ vọng: `AT-011 PASS` ... `AT-022 PASS`.

Test chạy trong transaction và kết thúc bằng `ROLLBACK`.

Nếu bất kỳ test nào fail: dừng gate 011 → 020, xác định lỗi contract/migration, reset database sạch, chạy lại 001 → 020 và acceptance suite.

## Verification result — Migration 011 → 020

- PostgreSQL: **18.6**
- Migrations 011 → 020: **executed successfully**
- Acceptance: [acceptance_011_020.sql](./acceptance_011_020.sql)
- Result: **AT-011 → AT-022 PASS (12/12)**
- Transaction: **ROLLBACK**
- AT-020 specifically verifies cross-organization session/device rejection using a device from Org B.
- Gate status: **PASS — Migration 021 → 030 may proceed.**
