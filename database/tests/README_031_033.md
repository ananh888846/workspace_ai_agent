# Acceptance 031 → 033

Kiểm thử production SQL trên PostgreSQL 18.6.

## Mục tiêu

- AT-031: Agent có tenant scope và organization FK.
- AT-032: Tên Agent unique trong cùng organization nhưng có thể trùng ở organization khác.
- AT-033: Agent capability mapping unique và cascade theo Agent.
- AT-034: Tool là global/shared catalog và tên Tool unique toàn hệ thống.

## Chạy

```powershell
Get-Content .\database\tests\acceptance_031_033.sql | docker exec -i workspace-ai-agent-postgres psql -U workspace -d workspace_ai_agent -v ON_ERROR_STOP=1
```

Acceptance kết thúc bằng ROLLBACK để không làm bẩn database test.
