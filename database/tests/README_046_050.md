# Acceptance 046 → 050

## Mục tiêu

Verify hậu V2.1 cho DBR-001 → DBR-007 trên PostgreSQL 18.6.

## Migrations

- 046 — Event / Activity Session / Activity user tenant integrity.
- 047 — Conversation owner integrity.
- 048 — Tool Run execution account context.
- 049 — Audit Log account authorization context.
- 050 — Resource identity semantics.

## Acceptance

- AT-053 — Cross-org Event user REJECT.
- AT-054 — Cross-org Activity Session user REJECT.
- AT-055 — Cross-org Activity user REJECT.
- AT-056 — Tool Run direct-owned account ACCEPT.
- AT-057 — Tool Run active delegated account ACCEPT.
- AT-058 — Invalid Tool Run account grant REJECT.
- AT-059 — Cross-org Audit account REJECT.
- AT-060 — Audit direct-owned account ACCEPT.
- AT-061 — Audit delegated account ACCEPT.
- AT-062 — Same local resource identity across different organizations ACCEPT.
- AT-063 — Duplicate account-backed resource REJECT.
- AT-064 — Duplicate local resource within organization REJECT.
- AT-065 — Agent Run cross-user Conversation REJECT.

## Cách chạy

~~~powershell
Get-Content .\\database\\migrations\\046_harden_activity_event_user_tenant_integrity.sql | docker exec -i workspace-ai-agent-postgres psql -U workspace -d workspace_ai_agent -v ON_ERROR_STOP=1
Get-Content .\\database\\migrations\\047_harden_conversation_owner_integrity.sql | docker exec -i workspace-ai-agent-postgres psql -U workspace -d workspace_ai_agent -v ON_ERROR_STOP=1
Get-Content .\\database\\migrations\\048_bind_tool_run_account_context.sql | docker exec -i workspace-ai-agent-postgres psql -U workspace -d workspace_ai_agent -v ON_ERROR_STOP=1
Get-Content .\\database\\migrations\\049_bind_audit_account_context.sql | docker exec -i workspace-ai-agent-postgres psql -U workspace -d workspace_ai_agent -v ON_ERROR_STOP=1
Get-Content .\\database\\migrations\\050_lock_resource_identity_semantics.sql | docker exec -i workspace-ai-agent-postgres psql -U workspace -d workspace_ai_agent -v ON_ERROR_STOP=1

Get-Content .\\database\\tests\\acceptance_046_050.sql | docker exec -i workspace-ai-agent-postgres psql -U workspace -d workspace_ai_agent -v ON_ERROR_STOP=1
~~~

Acceptance script kết thúc bằng ROLLBACK, vì vậy fixture test không được giữ lại.

**Gate:** chưa CLOSED cho đến khi chạy thực tế trên PostgreSQL 18.6 và runtime catalog verification PASS.


## Runtime catalog verification

Sau khi acceptance PASS, chạy thêm:

~~~powershell
Get-Content .\\database\\tests\\runtime_catalog_verification_046_050.sql | docker exec -i workspace-ai-agent-postgres psql -U workspace -d workspace_ai_agent -v ON_ERROR_STOP=1
~~~

Script kiểm tra constraints, columns, triggers và unique indexes trong `pg_catalog`/`pg_indexes`. Kết quả bắt buộc: `RV-001..RV-014 PASS`.
