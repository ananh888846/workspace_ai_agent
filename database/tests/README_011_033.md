# Acceptance 011 → 033

Integration acceptance cho production migrations 011 → 033.

Kiểm tra resource/device/session tenant chain; observation → event → activity; task; conversation/message/memory; knowledge → Qdrant mapping; agent/capability/tool; và cross-tenant denial ở DB layer.

## Chạy

```powershell
Get-Content .\database\tests\acceptance_011_033.sql | docker exec -i workspace-ai-agent-postgres psql -U workspace -d workspace_ai_agent -v ON_ERROR_STOP=1
```

Transaction kết thúc bằng ROLLBACK.